FROM cloudsuite/memcached:1.5.10
LABEL maintainer="Arash Pourhabibi Zarandi <arash.pourhabibi@epfl.ch>"

# the entry point is set to memcached in the base image
CMD ["-t", "2", "-m", "2048", "-n", "550"]
